#import "../Equipment/schema.graphql"

type Substrate implements Equipment {
    id: Int!
    predefined: Boolean!
    model: String!
    description: String
    image: String
}

type Query {
    substrates: [Substrate!]!
}

#import "../Brand/schema.graphql"

type Substrate {
    id: Int!
    predefined: Boolean!
    brand: Brand
    model: String!
    description: String
    image: String
}

type Query {
    substrates: [Substrate!]!
}

#import "../Brand/schema.graphql"

type Additive {
    id: Int!
    predefined: Boolean!
    brand: Brand
    model: String!
    description: String
    image: String
}

type Query {
    additives: [Additive!]!
}

#import "../Brand/schema.graphql"

type Filter {
    id: Int!
    predefined: Boolean!
    brand: Brand
    model: String!
    description: String
    image: String
}

type Query {
    filters: [Filter!]!
}

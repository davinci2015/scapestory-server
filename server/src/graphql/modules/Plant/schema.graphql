type Plant {
    id: Int!
    name: String!
    description: String
    image: String
    origin: String
    minHeight: Int
    maxHeight: Int
    position: String
    luminosity: String
    growthSpeed: String
    difficulty: String
}

type Query {
    plants: [Plant!]!
}

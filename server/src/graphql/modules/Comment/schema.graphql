# import User from '../User/schema.graphql'

type Comment {
    id: Int!
    content: String!
    parentCommentId: Int
    user: User!
}

input Pagination {
    limit: Int!
    offset: Int!
}

enum EntityType {
    AQUASCAPE
    IMAGE
}

type Query {
    comments(entity: EntityType!, entityId: Int!, pagination: Pagination): [Comment!]!
}

type Mutation {
    addComment(entity: EntityType!, entityId: Int!, content: String!, parentCommentId: Int): Comment
}
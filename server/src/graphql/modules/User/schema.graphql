type Query {
    me: User!
    user(id: Int!): User
    users: [User]!
}

type User {
    id: Int!
    email: String!
    slug: String!
    name: String!
    profileImage: String
    country: String
    youtubeLink: String
    instagramLink: String
    createdAt: String!
    updatedAt: String!
}

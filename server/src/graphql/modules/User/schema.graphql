type Query {
    me: User
    user(id: Int!): User
    users: [User]
}

type User {
    id: Int!
    email: String!
    username: String!
    name: String
    profileImage: String
    country: String
    createdAt: String!
    updatedAt: String!
}
type Brand {
    id: Int!
    name: String!
}

type Light {
    id: Int!
    predefined: Boolean!
    name: String
    brand: Brand
    model: String
    description: String
    image: String
}

type Query {
    lights: [Light]!
}